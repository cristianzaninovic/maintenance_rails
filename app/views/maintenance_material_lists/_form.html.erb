<%= form_with(model: maintenance_material_list) do |form| %>
  <% if maintenance_material_list.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(maintenance_material_list.errors.count, "error") %> prohibited this maintenance_material_list from being saved:</h2>

      <ul>
        <% maintenance_material_list.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :material_id, style: "display: block" %>
    <%= form.text_field :material_id %>
  </div>

  <div>
    <%= form.label :maintenance_id, style: "display: block" %>
    <%= form.text_field :maintenance_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
